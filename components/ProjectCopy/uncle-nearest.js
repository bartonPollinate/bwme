import Link from 'next/link'
//import styles from './Hero.module.scss';
//import { useEffect, useRef } from "react";

export default function Copy() {
  return (
    <>
      <p>Uncle Nearest Premium Whiskey honors the world’s first-known African American master distiller, Nearest Green. Their website was built and launched around the time I was first learning what HTML was. Nearly a decade later, I brought their Wordpress site into the 21 Century adding Gutenberg, custom blocks, accessibility features, and more scalable CSS styling.</p> 
      <p>The most complex feature on their site is a dynamic map showing the locations a person could find their whiskey. This map is fed by an API from their distributor but also supplemented with a custom json feed for more flexibility and performance.</p>
    </>
  );
}