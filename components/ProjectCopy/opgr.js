import Link from 'next/link'
//import styles from './Hero.module.scss';
//import { useEffect, useRef } from "react";

export default function Copy() {
  return (
    <>
      <p>Websites don't often have high stakes but for addiction services the user experience is so much more important than any marketing or commerce goal.</p>
      <p>OPGR integrates a live chat to give immediate help to those in need. This chat has a custom implementation that allows us to funnel users into alternate modes of communication on off hours so no one is left in the lurch during a potential crisis. There is also a custom plugin that allows users to search for locations with real gps data to get them the closest available location.</p>
      <p>Content is managed by the client so making an easy to use dashboard experience was paramount. From custom blocks to organizing data into various post types or settings, this site built to be useful not cumbersome. This site was a solo build with some functionality imported from their previous site.</p>
      <p>OPGR is a site that changes with the seasons. Designed for four separate colors schemes, this site generates four stylesheets seamlessly with each color injected effortlessly. The custom Gutenberg blocks were created around this as well which allows the images to change alongside the colors with just a single setting change.</p>
      
    </>
  );
}